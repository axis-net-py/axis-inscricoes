export default async function Login({searchParams}){const q=await searchParams;return <main className="wrap"><section className="hero"><div className="eyebrow">Área administrativa</div><h1>Axis Inscrições</h1></section><form className="card" method="post" action="/api/admin/login" style={{maxWidth:520}}><div className="field"><label>Senha administrativa</label><input name="password" type="password" required autoFocus/></div>{q?.error&&<p className="error">Senha inválida.</p>}<button className="button">Entrar</button></form></main>}
