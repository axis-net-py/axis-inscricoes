import {NextResponse} from 'next/server';import crypto from 'node:crypto';import {COOKIE,makeSession} from '@/lib/session';
export async function POST(req){const form=await req.formData();const supplied=String(form.get('password')||'');const expected=process.env.ADMIN_PASSWORD||'';const ok=supplied.length===expected.length&&supplied.length>0&&crypto.timingSafeEqual(Buffer.from(supplied),Buffer.from(expected));if(!ok)return NextResponse.redirect(new URL('/admin/login?error=1',req.url),303);const res=NextResponse.redirect(new URL('/admin',req.url),303);res.cookies.set(COOKIE,makeSession(),{httpOnly:true,secure:true,sameSite:'strict',path:'/',maxAge:8*60*60});return res;}
